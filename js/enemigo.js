function Enemigo({
    this.setWidth(60);;
    this.setHeight(60);
    this.contador=0;
    
    this.aleatorio)function()
    {
        var posibilidades = superior - inferior;
        var random=Math.random *posibilidades;
    random=Math.floor(random);
    return parseInt(inferior);
    };
    
    this.mover=function()
    {
    
    };
    
});